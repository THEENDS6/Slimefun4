# Additive / override content entry.
# Bundled legacy Slimefun item/category definitions now live in content/items/*.yml.
# Keep this file for local overrides. Use replace: true to override an existing category or item by id.

categories: []

items: []
